import { SizingBoxDirective } from './sizing-box.directive';

describe('SizingBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new SizingBoxDirective();
    expect(directive).toBeTruthy();
  });
});
